namespace Ludeka.Core.Enums;

public enum LanguageDependence
{
    None,   // 🟢 Nula (solo iconografía/simbología)
    Low,    // 🟡 Baja (frases cortas o texto visible)
    High    // 🔴 Alta (párrafos densos, requiere lectura fluida)
}
